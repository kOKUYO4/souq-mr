import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Guide anti-arnaque",
  description: "Comment acheter et vendre en toute sécurité sur NUQTA.MR — conseils, signaux d'alarme et système Escrow.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
